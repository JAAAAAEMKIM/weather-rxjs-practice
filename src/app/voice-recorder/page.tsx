import { useCallback } from 'react';
import { Recorder } from './Recorder';

export default function Weather() {
  return (
    <main>
      <div>Weather App</div>
      <Recorder />
    </main>
  );
}
